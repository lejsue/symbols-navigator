'use babel';

import SymbolsNavigatorView from '../lib/symbols-navigator-view';

describe('SymbolsNavigatorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
